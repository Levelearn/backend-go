# --- Server Configuration ---
# Port aplikasi berjalan (Default Gin biasanya 8080)
PORT=8080
# Mode: "debug" (untuk development) atau "release" (untuk production/deploy)
GIN_MODE=debug

# --- Database Configuration (MySQL) ---
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
# Kosongkan password jika menggunakan XAMPP default, atau isi jika ada password
DB_PASSWORD=
DB_NAME=levelearn_db

# --- JWT Configuration (Authentication) ---
# Gunakan string acak yang panjang dan rumit untuk keamanan
JWT_SECRET=rahasia_dapur_levelearn_jgn_disebar_2026
# Durasi token dalam jam (misal 24 jam)
JWT_EXPIRATION_HOURS=24

# --- Optional: Client URL (CORS) ---
# URL Frontend (Flutter/React) untuk izin akses API
CLIENT_ORIGIN=http://localhost:3000